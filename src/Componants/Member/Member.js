import React from 'react';
import imge from '../../image/logo.png';
import member from './Member.css'
const Member = (props) => {
    const {name,phone,email}=props.member;
    ////console.log(props.member)
    return (
        <div className=""> 
           
        </div>
    );
};

export default Member;